<div class = "container">

    <div class = "jumbotron text-center">
    
        <h1>MusicStudio</h1>
        <p>Welcome to MusicStudio, the new WebApp used by music professionals to help track their students
        <p>This is a VERY EARLY BETA! Be aware of many bugs and issues<p>
        <p>Currently there are no personal accounts.</p>
        
    </div>

</div>

<div class = "container text-center">

    <div class = "row">
    
    <% @teachers.each do |teacher| %>
        <% if @counter % 4 == 0 %>
            </div>
            <div class = "row">
        <% end %>
    
        <div class = "col-sm-4">
            <%= link_to(teacher_path(teacher), :class => "btn btn-default btn-lg active btn-block voffset2") do %>
                <h2><%= teacher.name %></h2>
                <h3><%= teacher.primaryInstrument %></h3>
            <% end %>
        
        </div>
    <% @counter += 1 %>
    <% end %>
    
    </div>

</div>